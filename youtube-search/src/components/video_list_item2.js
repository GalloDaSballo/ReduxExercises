/**
tKes the props and then declase vidoe from props.videos
*/
import React from 'react';

const VideoListItem = (props) => {
  const video = props.video;
  return <li>Video</li>
};

export default VideoListItem;
